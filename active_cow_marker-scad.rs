//! Active cow marker — see `active_cow_marker.scad` (OpenSCAD).

use anyhow::Result;

fn main() -> Result<()> {
    eprintln!("Use active_cow_marker.scad — openscad -o active_cow_marker.stl active_cow_marker.scad");
    Ok(())
}
